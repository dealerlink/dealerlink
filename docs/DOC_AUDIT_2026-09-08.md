# Documentation Drift Audit — Stage F Day 23

> **Date:** 2026-09-08
> **Tree:** branch `day-23-agent-roster`, `main` @ `4b40bcd`
> **Produced by:** the `doc-auditor` subagent (`.claude/agents/doc-auditor.md`),
> its first run. Read-only: no file was edited and no state-changing command was
> run. Test counts were read from CI run `34134354351`, not by running a suite.
> **Nothing in this report has been fixed.** The agent reports drift; the main
> thread fixes it, and Day 23 was infrastructure-only. See DEV.100.
>
> **Transcription note:** the agent had no write tool, so the main thread saved
> its returned report here verbatim in substance. Findings, quotes, line numbers
> and commands are the agent's.

---

## DRIFT — a reader acting on this today would be wrong

### 1. `docs/STRUCTURE.md` describes a tree that no longer exists

`CLAUDE.md:11` sends every new session here first.

| Claim                                                                        | Reality                                                                                                                     |
| ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `:14-15` — `.github/workflows/ci.yml` + `deploy.yml`                         | `ls .github/workflows/` → **only `verify.yml`** (Day 22, `f667c68`; jobs `checks`/`test`/`e2e`)                             |
| `:33`, `:49` — `app/api/trpc/[trpc]/` and `lib/trpc/`                        | `apps/web/app/api` holds `health`, `internal/sentry-test`, `webhooks/resend`. tRPC removed Day 5 (ADR-011)                  |
| `:36-37` — `webhooks/resend-inbound/` + `resend-delivery/`                   | one route: `apps/web/app/api/webhooks/resend/`                                                                              |
| `:85-91` — `packages/db/schema/…` with `email.ts` and a `logs/` subdirectory | `packages/db/**src**/schema/` — flat, no `logs/`, no `email.ts`; files are `email-delivery-log.ts`, `generated-document.ts` |
| not mentioned at all                                                         | `apps/web/lib/queries/` (10 files) — the pattern `CLAUDE.md:123` names as tRPC's replacement                                |

File mtime `Sep 3` — untouched since Stage A.

**Cost if acted on:** a session navigating by this doc greps for files that do
not exist and re-derives the layout by hand; worst case it _creates_
`ci.yml`/`deploy.yml` alongside the real `verify.yml` and breaks the merge gate.

### 2. Three pilot-facing docs promise bank details on a document that does not exist

- `docs/PILOT_GETTING_STARTED.md:135` — "print on the footer of every tax
  invoice… Do this **before** you create your first invoice"
- `docs/PILOT_ONBOARDING_PRODUCTION.md:59` — "must enter real bank details in
  Settings before issuing any tax invoice"
- `docs/RUNBOOKS.md:21` — "Bank account details (printed on tax invoices)"

**Reality:** `docs/TAX_INVOICE_AUDIT.md:21` — "# VERDICT: **DOES NOT EXIST**";
`apps/workers/src/jobs/render-pdf.ts` throws on `documentType: 'invoice'`.
`docs/SECURITY_AUDIT.md:831-837` already logged this on Day 19 and said it
"should be corrected with F.6" — but F.6's task notes carry no such item, and
F.6 is `pending` at days 37–39.

**Compounding it:** `docs/stage-f-tasks.json` F.2b (status `complete`) claims
_"bank-details copy corrected to the 4 docs it prints on"_. `git show 21a0fa6
--stat` shows that commit touched only the **admin UI** strings
(`apps/web/app/admin/tenants/…`, 2 lines each) and `docs/RUNBOOKS.md` (+43
lines, unrelated) — none of the three pilot docs. A reader would conclude this
was fixed.

**Cost if acted on:** a live pilot tenant waits for an invoice screen that will
not ship for ~30 days; a session "verifying" F.2b finds the claim false and
re-opens a closed day.

### 3. `CLAUDE.md` §3's locked-stack table names five picks installed nowhere

| `CLAUDE.md` | Claim                                                 | Reality (`grep -rn … --include=package.json`)                                                                                                                                                                           |
| ----------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `:117`      | Tables — **TanStack Table v8** + **TanStack Virtual** | no `@tanstack/*` in any package.json                                                                                                                                                                                    |
| `:121`      | Server cache — **TanStack Query**                     | absent                                                                                                                                                                                                                  |
| `:119`      | Charts — **Tremor** + custom SVG                      | absent (custom SVG only)                                                                                                                                                                                                |
| `:138`      | Email — **Resend** SDK, templates via `react-email`   | no `resend`, no `react-email`. `apps/workers/src/email/resend-client.ts:4` — "no SDK dependency", raw HTTPS to `https://api.resend.com/emails`. `tenant-welcome.ts:5` says react-email arrives with the second template |

`README.md:58` repeats "TanStack Table · Tremor".

**Cost if acted on:** a session writing a table or dashboard imports a package
that is not there, or installs it believing it is the locked choice.

### 4. `CLAUDE.md:3` is four stages behind and contradicts line 519 of the same file

- `CLAUDE.md:3` — "Last reviewed: 2026-05-27 — **Stage C COMPLETE** … **Next:
  Stage D** (production infra) — see `docs/STAGE_D_HANDOFF.md`."
- `CLAUDE.md:519` — "_Last updated: September 2026 · … · §10 added Stage F Day 23_"

**Reality:** `git tag` → `stage-b-complete`, `stage-c-complete`,
**`stage-d-complete`**; the project is on Stage F Day 23; Stage E shipped a live
production tenant (`4946e09`).

**Cost if acted on:** it is the first line of the authoritative guide. A fresh
session opens `docs/STAGE_D_HANDOFF.md` and plans infra work completed on
2026-05-31.

### 5. `CLAUDE.md` still specifies tRPC in three places after §3 records its removal

`:135` — "Queries | **tRPC procedures** | For dashboards, search, exports";
`:304` — "Permission enforcement: **at the tRPC procedure / Server Action
level**"; `:81` — the architecture diagram's Process 1 box reads "Next.js SSR +
Server Actions + **tRPC** + Lucia + Drizzle".

**Reality:** `:123` — "tRPC removed Day 5 … ADR-011". No `trpc` dependency, no
`app/api/trpc/`, no `lib/trpc/`.

### 6. `docs/TESTING.md:8` names a test dependency never installed

**Claim:** "Integration | **Vitest + testcontainers-postgres** | Database
operations against a real Postgres with RLS active".

**Reality:** `grep -rn testcontainers --include=package.json .` → no hits.
Integration tests run against the compose Postgres locally and a per-job GitHub
Actions **service container** in CI.

**Cost if acted on:** a session adding a DB test installs testcontainers and
rewires a harness that already works.

### 7. `docs/LOGGING.md` — two of the six Postgres log tables do not exist

`:10` — "**Email content** (`email_log`)"; `:14` — "**Document generation**
(`document_log`)".

**Reality:** `grep -rn "email_log\|document_log" packages/db` returns only a
comment in `packages/db/src/schema/email-delivery-log.ts:9` — "(`email_log`,
full body) **ships later**". Actual log tables: `audit_log`, `auth_events`,
`access_log`, `email_delivery_log`; document rendering is recorded in
`generated_documents`. `CLAUDE.md:89`'s "6 log tables" inherits the overcount.

**Cost if acted on:** an RLS or retention task written against `email_log` /
`document_log` fails at migration time; a compliance answer names tables that
hold nothing.

### 8. `CLAUDE.md` §8 states logos are stored in DO Spaces; Spaces is not wired up

`:371` — "Stored in DO Spaces; URL in `tenant_settings.logo_url`"; `:387` —
"Logo (URL in Spaces)".

**Reality:** no S3/Spaces client in any package.json. `'spaces'` exists only as
an unreachable branch of a storage union
(`apps/web/lib/queries/generated-documents.ts:13`, `…/download-pdf.ts:23` —
"set when storage is 'spaces' (**Stage D**)"). `apps/web/lib/admin/schemas.ts:173`
accepts `logoUrl` as a **100 000-char string** — an inline data URI.
`docs/stage-f-tasks.json` F.45 is `deferred`.

### 9. `CLAUDE.md:62` cost target is 2.4× off the invoice-reconciled figure

**Claim:** "**Phase 1 cost target:** ~$40/month operational".
**Reality:** `COSTS.md:19` — "Pilot baseline | 1 (live) | **~$96**", `:11`
"reconciled against actual DO invoice (June 2–8, 2026)".

**Cost if acted on:** F.35 ("Downsize production") is `pending` at day 25 —
sizing that day against $40 rather than $96 mis-scopes the cut.

### 10. `CLAUDE.md:153` states the wrong production worker size — the fact F.35 turns on

**Claim:** "Web: basic-xs (buildpack). **Workers: basic-xxs** (custom Dockerfile
per ADR-013)."
**Reality:** `doctl apps spec get d8a25cb8-e4cb-4035-8413-6baab72398cd` → workers
`instance_size_slug: basic-xs`. Only _staging_ is `basic-xxs`. `COSTS.md:40`
and `:51` have it right.

### 11. Smaller broken references

- `README.md:52` — "`docs/DECISIONS.md`". The file is `/DECISIONS.md` at repo
  root; `docs/DECISIONS.md` does not exist.
- `CLAUDE.md:67` — "`Dealerlink.html` + `Dealerlink-print.html` … the visual
  source of truth". The files are `docs/Distribyte.html` /
  `docs/Distribyte-print.html` — the names `CLAUDE.md:34` itself uses. No
  `Dealerlink*.html` exists in any commit.
- `CLAUDE.md:85` — "Process 2 (Node 20, **pm2-managed**)". No `pm2` anywhere;
  `apps/workers/Dockerfile:66` → `CMD ["pnpm","--filter","workers","start"]`.
- `STAGE_D_PROMPTS.md:55` — "**Operator follow-up still open:** Update
  RESEND_FROM_EMAIL …". Closed; `docs/STAGE_D_HANDOFF.md:99` carries the
  correcting note, this file does not.
- `docs/STAGE_F_BUILD_v3.md:359` — "Continue from **DEV.92** (Day 21)".
  `DEVIATIONS.md` now runs to DEV.100.
- `docs/STANDARDS.md:20` — "`packages/tax/index.ts`". It is
  `packages/tax/src/index.ts`.

---

## STALE BUT HARMLESS — correctly labelled historical records

- `docs/SECURITY_AUDIT.md:1-10` — dated "Stage C, Day C.4", with every later
  disposition appended and dated (`:693` C.4, `:714` D.2, `:750` D.1, `:771`
  Day 19). The §10 status column is current.
- `docs/STAGE_F_HANDOFF.md:3` — self-labelled "**HISTORICAL (Day 19 kickoff).**
  Superseded from Day 22 onward." Its "30 tasks" is wrong today (55) but the
  banner says so.
- `docs/STAGE_F_BUILD_v3.md:340-348` — references `POST_PILOT_WORK_ORDER.md` and
  `docs/STAGE_F_BUILD_v2.md`, which do not exist; both are explicitly documented
  as gone / never committed.
- `docs/TAX_INVOICE_AUDIT.md:10-14` — says outright that its day allocations are
  v2-era and left unedited as a record.
- `DEVIATIONS.md` — append-only by design. DEV.87/DEV.88 read as open; later
  entries close them, as intended.
- `docs/STAGE_D_HANDOFF.md:349` — D.2 row lists the RESEND_FROM_EMAIL follow-up
  as open, but `:99` in the same file annotates it closed.

---

## BLOCKED

- **Nothing was blocked by auth.** `doctl auth list` → `default (current)`; both
  `apps spec get` calls returned; `gh run list/view` returned.
- **Test counts for this branch could not be read from CI** — `gh run list
--branch day-23-agent-roster` returns no runs. Counts below come from the
  newest successful run on `main` (`34134354351`, `4b40bcd`), the correct
  baseline for every count quoted in the docs. No suite was run.

---

## CHECKED AND AGREEING

This section matters as much as the drift.

- **Test counts — exact match.** `docs/STAGE_F_BUILD_v3.md:357` claims "560
  vitest, 65 verify". CI run `34134354351`: `packages/tax` 51 + `packages/schemas`
  68 + `packages/db` 172 + `apps/web` 203 + `apps/workers` 40 + `scripts` 26 =
  **560** across 50 test files; e2e "Running **65** tests" → 2 flaky / 63 passed.
  23 `.spec.ts` files on disk hold those 65.
- **`docs/SECURITY_AUDIT.md` F-1…F-9 — all nine re-checked against code, no
  stale-open finding.** F-1 closed (`apps/web/package.json:40` pins
  `"next": "14.2.35"` exact, lockfile agrees, `0c20952` present). F-2 closed
  (`apps/web/next.config.mjs:47-61` — CSP, `X-Frame-Options: DENY`, HSTS,
  Referrer-Policy; `8c205ad`). F-3 closed (`apps/web/lib/auth/actions.ts`
  :70/:125/:142/:161/:191 wire `peekRateLimit`/`checkRateLimit`/`isLockedOut`/
  `resetRateLimit`; `0d6638d`). F-4 closed (`drizzle-orm ^0.45.2` across web,
  workers and db; `drizzle-kit ^0.31.10`; `aa21da4`, `fbb5fed`, `934d3c9`,
  `e787efc` all present). F-5 still open (permissive INSERT/UPDATE policies in
  `packages/db/src/rls/{audit-log,access-log,auth-events,email-delivery-log}.sql`).
  F-8 still open (`docs/pilot/credentials-cheatsheet.md` still committed).
  F-9 still open (`apps/web/lib/admin/schemas.ts:173` — no content-type check,
  no sanitiser). Severity counts at `:19-22` arithmetically correct.
  **This is the exact failure that produced a wrong Day 19 plan. It has not
  recurred.**
- **`.do/app.yaml` vs live staging `77edf06b-…`** — env key names byte-identical
  in order (`diff` clean); `instance_size_slug` web `basic-xs` / workers
  `basic-xxs`; `instance_count` 1/1; `branch: main`; `deploy_on_push: true`;
  health check, `http_port`, DB cluster and all three domains match. The only
  live-side addition is the DO-derived `ingress:` block, which
  `scripts/sync-app-spec.mjs` is documented to preserve. No secret values printed.
- **`.do/app.production.yaml` vs live production `d8a25cb8-…`** — env key names
  byte-identical in order; every non-SECRET value matches exactly, including
  `RESEND_FROM_EMAIL: noreply@dealerlink.in` on **both** web and workers,
  `AXIOM_DATASET: dealerlink-production`, pools 10/5/2 and 5/2/3,
  `PDF_EAGER_WARM: 'true'`; sizes, counts, branch, `deploy_on_push`, ingress and
  `app.dealerlink.in` PRIMARY + `dealerlink.in` ALIAS `wildcard: true` all match.

  > **F.36 is closeable on evidence.** Its premise — a `RESEND_FROM_EMAIL` drift
  > between the committed spec and live config — is **phantom**, and the wider
  > non-SECRET diff the task was reserved for is also clean. Operator decision,
  > not taken here.

- **`CLAUDE.md` §7 "Explicitly NOT used"** — none of Redis, Meilisearch, Zustand,
  Auth.js, Recharts, Instrument Serif, Turborepo, Kubernetes, PostHog or Twenty
  CRM appears in any package.json or import.
- **`CLAUDE.md` §3 picks that do hold** — Next 14 App Router; TS strict with
  `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` (`tsconfig.base.json`);
  Tailwind `^3.4.17`; Inter + IBM_Plex_Mono via `next/font/google`
  (`apps/web/app/layout.tsx:4`); dnd-kit; react-hook-form + `@hookform/resolvers`;
  Zod (root override `3.25.76`); Lucia `^3.2.2` + `@node-rs/argon2`; Drizzle;
  pg-boss `^10.1.5`; `puppeteer-core` + `@sparticuz/chromium` in workers only;
  Node `20.18.0` (`.nvmrc`, `Dockerfile:16`); Postgres 16; `pg_trgm` + GIN
  trigram indexes on dealer name/GSTIN/code, product name/manufacturer/model and
  inventory serial (`packages/db/migrations/0004_huge_king_cobra.sql:155-163`);
  `lib/queries/` present.
- **`docs/stage-f-tasks.json` statuses vs what shipped** — every `complete` task
  verified: F.1 (`rate-limit.test.ts` 8, `login-enumeration.test.ts` 6,
  `verify-day-19.spec.ts` 4), F.2 (`docs/TAX_INVOICE_AUDIT.md`), F.2b
  (`packages/db/.env.test`, `apps/web/lib/auth/cookie-security.ts`,
  `scripts/verify-deploy.mjs`, migration `0017_friendly_logan.sql`) — **except**
  the pilot-docs claim in finding 2 — F.2a (`docs/DRIZZLE_UPGRADE_ANALYSIS.md`,
  all four commits, versions on disk), F.33 (`.github/workflows/verify.yml`,
  R22 + R23). No `pending` task found already done: F.34's file still exists,
  production is still un-downsized, F.37 not started, F.52 corroborated by the
  2 flaky specs in the last CI run.
- **Statuses vs the sync script's allowed set** — `scripts/sync-project-plan.ts:62-69`
  defines exactly six and rejects anything else at parse time. The JSON uses four.
- **`PROJECT_PLAN.md` generated block vs `docs/stage-f-tasks.json`** — every task
  matches on id, status symbol and day range inside the `STAGE_F_TASKS` markers,
  including the Day 23 re-sequencing.
- **`CLAUDE.md` §10.5 vs `.claude/settings.json`** — the deny list is exactly as
  described.
- **`CLAUDE.md` §10 roster vs disk** — `docs/RUNBOOKS.md:1266` R24 exists and
  names all six agents; all six files present in `.claude/agents/`.
- **`CLAUDE.md` §5 allowed GST rates** — "`0, 3, 5, 12, 18, 28`" matches
  `packages/db/migrations/0017_friendly_logan.sql` verbatim on all four tables.
- **`CLAUDE.md` Reading Order (`:11-20`)** — all ten cited `docs/*.md` exist.
- **CI job names vs the enforced gate** — `verify.yml` defines `checks` / `test`
  / `e2e`, matching the three required status checks in `CLAUDE.md` §10.3 and R22.

---

## Adjacent notes — not doc drift

- `@logtail/pino` is still a declared dependency of both `apps/web` and
  `apps/workers` although DEV.79 removed the transport. The code comments at
  `apps/web/lib/observability/logger.ts:16` and
  `apps/workers/src/observability/logger.ts:7` explain why it is unused. An
  unused dependency, not a false claim.
- The Day 23 re-sequencing shifted every dated task +1 rather than adding a
  Day 23 task. (Resolved after this audit ran: F.54 was appended for Day 23.)
