# Dependencies
node_modules/
.pnpm-store/
# Builds
.next/
dist/
build/
*.tsbuildinfo
# Env
.env
.env.local
.env.*.local
!.env.example
# Logs
*.log
logs/
npm-debug.log*
pnpm-debug.log*
# OS
.DS_Store
Thumbs.db
desktop.ini
# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
# Docker
docker-data/
# Test
coverage/
.nyc_output/
test-results/
playwright-report/
# Secrets safety net
*.pem
*.key
secrets/
# Belt-and-braces: actual staging-secrets.txt lives at
# C:\Users\rohit\.dealerlink\staging-secrets.txt (outside the repo). This
# guards against an accidental copy landing at repo root.
/staging-secrets.txt
# Rendered App Platform spec — contains injected secret values, never commit.
.do/app.rendered.yaml
# Claude Code runtime artifacts
.claude/scheduled_tasks.lock
# Load-test captured session cookies (live session tokens — never commit)
scripts/load-test/.sessions.json